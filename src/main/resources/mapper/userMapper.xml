<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.board.menus.mapper.UserMapper">  
 	<select id="getUserList" resultType="com.board.menus.domain.UserDTO">
 		SELECT USERID, PASSWD, USERNAME, EMAIL, UPOINT, INDATE
 		FROM TUSER
 	</select>

	<insert id="insertUser">
		INSERT INTO TUSER(USERID, PASSWD, USERNAME, EMAIL, UPOINT, INDATE)
		VALUES(#{userid},#{passwd},#{username},#{email},#{upoint},#{indate})
	</insert>
 		
 	<delete id="deleteUser">
 		DELETE FROM TUSER
 		WHERE USERID = #{userid} 
 	</delete>
 	
 	<select id="getUser">
 		SELECT * FROM TUSER
 		WHERE USERID = #{userid}
 	</select>
 	
 	<update id="updateUser">
 		UPDATE TUSER
 		SET PASSWD = #{passwd},
 			  USERNAME = #{username},
 			  EMAIL = #{email}
 		WHERE USERID = #{userid}
 		
 	</update>
</mapper>

